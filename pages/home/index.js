import {
    Typography,
    Divider,
    Box,
    Tabs,
    Tab,
    useMediaQuery
} from '@mui/material';
import { useTheme } from '@mui/material/styles';

export default function Home({ breakpoints }) {
    return (
        <div>
            Home
        </div>
    );
}